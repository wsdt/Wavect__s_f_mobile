import * as de from './de.json'

// TODO: Find solution for making this file unnecessary (dynamically import the whole directory)

/** Fallback if not available lang fits */
export const fallbackLanguagePack = {languageTag: 'de', isRTL: false}


export default {
    de,
}
