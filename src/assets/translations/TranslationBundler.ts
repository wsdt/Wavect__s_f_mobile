import * as de from './langs/de.json'

/** Fallback if not available lang fits */
export const fallbackLanguagePack = { languageTag: 'de', isRTL: false }

/** Export here all languages which should be available */
export default {
    de,
}
